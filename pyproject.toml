[build-system]
requires = ["setuptools", "wheel", "Cython>=3.3.0,<3.4"]
build-backend = "setuptools.build_meta"


[tool.cibuildwheel]
build = "*p310-* *p311-* *p312-* *p313-* *p314-*"
build-frontend = "build"
build-verbosity = 1
test-requires = [ "pytest" ]
test-command = "pytest {project}"
